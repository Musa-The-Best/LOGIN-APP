     regenBtn?.addEventListener('click', () => { formError.textContent='';
    setTimeout('empty'); currentConcept=null; document.querySelector('
    .form-panel')?.scrollIntoView({behaviour:'smooth'}); })